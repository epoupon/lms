<?xml version="1.0" encoding="UTF-8" ?>
<messages xmlns:if="Wt.WTemplate.conditions">
<!--FORMS message blocks-->

<message id="Lms.Admin.InitWizard.template">
	<div class="container">
		<form class="row mt-3 justify-content-md-center g-3">
			<div class="col-lg-6">
				<div class="row g-3">
					<h2>${tr:Lms.Admin.InitWizard.header}</h2>
					<div class="col-12">
						<label class="form-label" for="${id:admin-login}">
							${tr:Lms.login}
						</label>
						${admin-login class="form-control"}
						<div class="invalid-feedback">
							${admin-login-info}
						</div>
					</div>
					<div class="col-lg-6">
						<label class="form-label" for="${id:password}">
							${tr:Lms.password}
						</label>
						${password class="form-control"}
						<div class="invalid-feedback">
							${password-info}
						</div>
					</div>
					<div class="col-lg-6">
						<label class="form-label" for="${id:password-confirm}">
							${tr:Lms.password-confirm}
						</label>
						${password-confirm class="form-control"}
						<div class="invalid-feedback">
							${password-confirm-info class="help-block"}
						</div>
					</div>
					<div class="col-12">
						${create-btn class="btn btn-primary"}
					</div>
					${info class="alert alert-success mt-3"}
				</div>
			</div>
		</form>
	</div>
</message>

</messages>
